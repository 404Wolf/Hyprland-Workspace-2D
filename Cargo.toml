[package]
name = "workspace2d"
version = "0.1.0"
edition = "2021"

[dependencies]
hyprland = "0.3.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libc = "0.2"
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
strip = true
lto = true
